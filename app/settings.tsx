import { Text, View } from "react-native";

export default function SettingsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-sky-50">
      <Text className="text-lg font-bold text-sky-900">⚙️ Settings Screen</Text>
    </View>
  );
}
